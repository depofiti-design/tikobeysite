name: Supabase Keepalive

on:
  schedule:
    - cron: '41 5 */3 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST API to prevent free-tier auto-pause
        run: |
          curl -sf "https://rfuwnjsyulpmdyespacd.supabase.co/rest/v1/sponsors?select=id&limit=1" \
            -H "apikey: sb_publishable_BeRLk3mCeFsA2MqHPKQ0CA_CvbzLdyd"
